
import React from 'react';
import ReactDOM from 'react-dom';
import Index from './DocumentationModal';

ReactDOM.render(
    <React.StrictMode>
        <Index />
    </React.StrictMode>,
    document.getElementById('documentation-modal-root')
);
